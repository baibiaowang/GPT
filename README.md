# 股票判断机 · GPT 专用仓库

这是股票判断机新版的独立开发仓库。

- 仅用于本 App 的新版本开发、测试与 APK 构建。
- 数据源地址和激活码在 App「设置」中分开配置。
- 应用更新地址固定指向本仓库 `baibiaowang/GPT`。


## Build

GitHub Actions 会自动构建 APK，并同步 `latest` 发布资产供 App 内更新检查使用。
